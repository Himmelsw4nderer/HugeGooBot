/**
 * Basic response if someone does not hace the permissuns
 * @returns response
 */
function noPermissions() :string {
    return `You do not have the permissions to do that`
}

export {noPermissions}